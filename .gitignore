# environment
.env
venv/

# python
__pycache__/
*.pyc

# persona library (accumulated across runs, not source-controlled)
personas_library.json

# test artifacts
screenshots/
reports/
runs/

# session summaries (private working notes — not shared history)
session_summaries/

# raw design-mode hand-offs (source-of-truth lives in templates/ after split)
design_bundle/

# obsidian vault copies (snapshotted ~weekly, not source of truth)
vault_storage_artifacts/

# claude code per-machine permission allowlist (local, not shared)
.claude/settings.local.json

# optional but common
.pytest_cache/
.DS_Store